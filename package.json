{
  "name": "le5le-ssr",
  "version": "1.0.0",
  "description": "",
  "main": "server/ws_server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "private": true,
  "dependencies": {
    "express": "^5.1.0",
    "ws": "^8.18.3"
  }
}
